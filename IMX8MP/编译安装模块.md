```
make distclean
make myd_imx8mm_defconfig
LDFLAGS="" CC="$CC"
make dtbs Image modules -j16
make INSTALL_MOD_STRIP=1 modules_install INSTALL_MOD_PATH=./modules
```
INSTALL_MOD_STRIP=1
去除内核模块的调试信息，生成的 .ko 文件将不包含符号表和其他调试信息，从而显著减小文件大小。